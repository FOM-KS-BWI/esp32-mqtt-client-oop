#ifndef FIRMWARE_H
#define FIRMWARE_H 

#include <Arduino.h>
#include <mqtt.h>
#include <WiFi.h>

class Firmware{
    private:
    ..../*data */
    public: 
    Firmware(/* args */);
    ~Firmware(); 
};

Firmware::FIRMWARE(/* args */){
}
Firmware::FIRMWARE(){
}

